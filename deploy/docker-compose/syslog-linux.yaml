version: "3.8"

services:
  mongodb:
    logging:
      driver: syslog
      options:
        syslog-facility: local0
        tag: mongo

  lovechedule-server:
    logging:
      driver: syslog
      options:
        syslog-facility: local0
        tag: lovechedule-server

  notification-server:
    logging:
      driver: syslog
      options:
        syslog-facility: local0
        tag: notification-server

  redis:
    logging:
      driver: syslog
      options:
        syslog-facility: local0
        tag: redis
